#ifndef PARSER_H
#define PARSER_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "token.h"

// Might change return type to custom type refering if the parsing was succseful or not
void begin(Token* tk);


#endif // !PARSER_H