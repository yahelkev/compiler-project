#ifndef TOOLING_H
#define TOOLING_H
#include <stdlib.h>
#include <string.h>
void remove_spaces(char* s);
char* substr(char* s, int i, int l); // s string to match, i index to find, l length of substr
#endif // !TOOLING_H